# 项目重构路线图

日期：2026-09-18  
状态：阶段 0–3 已完成；阶段 4–5 待实施。更新于 2026-09-20。

阶段 3 交付：[内容、路由与语言报告](docs/phase3/2026-09-20/README.md)、[内容维护指南](docs/content.md)。作品数据化、详情模板、语言持久化与页面元信息已完成；本地回退节点为 `codex/phase-3-checkpoint`。

阶段 2 交付：[共享布局与响应式 UI 报告](docs/phase2/2026-09-20/README.md)。统一自然流页头、Grid 卡片、字体与容器，首页正文和 3D 分区；本地回退节点为 `codex/phase-2-checkpoint`。

阶段 1 交付：[稳定性与部署路径报告](docs/phase1/2026-09-18/README.md)。已修复首页生命周期、异常回退、导航与静态部署路径，保留本地回退节点 `codex/phase-1-checkpoint`。

阶段 0 交付：[基线报告](docs/baseline/2026-09-18/README.md)、[问题与内容清单](docs/baseline/2026-09-18/inventory.md)、[截图](docs/baseline/2026-09-18/screenshots.md)、[性能](docs/baseline/2026-09-18/performance.md)、[测试记录](docs/baseline/2026-09-18/tests.md)。已建立本地回退节点 `codex/phase-0-checkpoint`，补充 Playwright 冒烟和可重复的采集脚本。

## 1. 目标与范围

把现有个人作品网站改造成布局稳定、手机可用、跨浏览器可靠、内容易维护的 Vue 应用。保留 PANNIC 品牌、深色背景、青绿色强调色和首页 3D 视觉特色；优先保证内容阅读与导航，再提供动画增强。

本轮“多平台”指 Windows/macOS 桌面浏览器、iOS Safari、Android Chrome，以及不同屏幕尺寸和输入方式；不包含原生 App、小程序、Electron 或离线 PWA。

- 保留 Vue 3、Vue Router、Vite 8；不为重构替换框架或引入大型 UI 库。
- 现有作品、图片、外链、中英文内容及可用页面均应保留；缺失内容单独记录，不编造项目介绍。
- 延续现有 Vite 升级和本地启动入口，避免覆盖当前尚未提交的改动。
- 使用渐进式迁移，每个阶段独立验证、可回退；不一次重写全部页面。
- 默认继续服务 GitHub Pages 子路径 `/Vite-Homepage-24/`，同时验证根路径部署。

## 2. 当前代码审查结果

以下“已确认”来自源码检查；实际设备上的表现、帧率和性能指标需要在阶段 0 建立基线，不能将推断当成实测结果。

| 优先级 | 已确认的问题与位置 | 影响及处理方向 |
| --- | --- | --- |
| P0 | `src/views/Home.vue` 在 `window.onresize` 中执行 `location.reload()`，离开页面时未清除此处理器 | 窗口变化会刷新页面；移除刷新逻辑，以容器尺寸更新布局和渲染器 |
| P0 | `src/three/home.js` 初始化连续调用两次 `setupThree()`，卸载仅取消动画帧 | 存在多余渲染器及 GPU 资源泄漏风险；实例化管理并完整释放资源 |
| P0 | 模型异步加载成功后直接启动动画，没有卸载状态保护；`document.ondblclick` 未恢复 | 快速离开首页后可能重启动画，事件影响其他页面；取消或忽略过期加载并解绑事件 |
| P1 | `main.js`、`style.css` 与多个组件广泛使用 `--vsr`、`vh` 决定字体和尺寸；主要断点为 `max-aspect-ratio: 1/1.7` | 字号随短边过度缩小，横屏、平板、分屏覆盖不足；建立移动优先的字体和容器体系 |
| P1 | `MenuSection.vue` 使用绝对定位、固定高度、DOM 测高；注册和移除 resize 时传入不同匿名函数 | 长文案容易溢出，监听无法正常移除；用自然文档流和 Grid 替代测高 |
| P1 | `About.vue`、`Works.vue`、`Projects.vue` 重复固定页头与一次性测高占位逻辑 | 语言或尺寸变化后占位高度可能失效；抽出共享页面布局 |
| P1 | `PageHeader.vue` 多层 fixed 定位；`Page.vue` 使用 `200vh` 占位和 `overflow-x: hidden` | 页头与正文高度强耦合；移除占位布局，不用裁剪掩盖溢出 |
| P1 | `MenuSection.vue` 用点击 div 导航，图片缺少 alt，标题 nowrap 且裁剪；首页禁止内容选择 | 键盘、触屏和长标题体验不足；补齐语义、焦点、换行与复制能力 |
| P1 | `/works/gcs` 为子路由，但 `Works.vue` 没有嵌套 `router-view`；详情内容不完整 | 路由存在但无法正常展示详情；迁移为完整详情路由或明确占位状态 |
| P1 | `SubRight.vue` 的“主页”按钮调用 `router.back()` | 直接访问页面时无法确保回首页；明确区分返回与主页导航 |
| P1 | 静态资源混用 `./image/...`、`/image/...`、`./cat.gltf` | 子路径部署及嵌套路由存在资源寻址风险；统一资源 URL 策略 |
| P1 | `i18n.js` 读取本地语言设置，但 `LanguageSwitch.vue` 切换时未写入；文案混合硬编码与翻译 key | 语言切换不完整、刷新不持久；收敛为单一语言状态并补齐翻译 |
| P1 | 首页同步引入 Three.js 与多个后处理效果；模型约 2.59 MB，多张图片超过 1 MB | 首屏网络与 GPU 压力较高；延迟加载、媒体优化和质量降级 |
| P2 | `App.vue` 无用导入，`Three.vue` 为空，保留模板组件、调试入口和日志 | 意图不清、维护成本高；确认引用后清理 |
| P2 | `dist` 虽被忽略，仍有文件被 Git 跟踪；未发现仓库 CI 工作流和测试脚本 | 构建产物易混入改动；明确部署产物来源并建立验证流程 |

上一轮验证：Vite 8.3.0 生产构建成功；主 JS 约 745.54 kB，gzip 约 216.98 kB，存在大块提示。`npm audit` 报告旧 `vue-i18n` 依赖链有 4 项漏洞。以上仅作起始记录，实施前重新测量。

## 3. UI 与适配设计原则

### 布局与视觉

- 先完成 360px 手机布局，再扩展平板和桌面；320px 为窄屏兜底。
- 使用统一 `PageContainer`：最大内容宽度、居中布局、可变边距。超宽屏不无限拉宽文字。
- 正文字号基于 rem，建议默认 1rem、行高 1.5–1.7；标题用具有上下限的 `clamp()`。不再将视口短边作为所有字体的基准。
- 以约 40rem、64rem 为初始布局断点，依据内容拥挤程度调整；宽高比仅用于装饰，不承担主要适配判断。
- 作品卡片在窄屏纵向排列，宽屏图文分栏；图片通过 `aspect-ratio`、`object-fit` 控制，不依赖 JS 设高。
- 统一标题层级、间距、按钮、标签、边框、焦点和层级 token；全局样式只负责基础规则，组件样式局部生效。
- 页头优先采用自然流或 sticky；装饰与正文分离。允许正文滚动，避免嵌套滚动容器。
- 手机视口使用合适的 svh/dvh 与回退值，并处理安全区；真机验证地址栏收缩、横竖屏切换和虚拟键盘。

### 输入、可访问性与语言

- 导航使用 `RouterLink`/链接，动作使用 button；无链接的作品卡片不显示虚假的可点击状态。
- 主要点击区域目标至少 44×44 CSS px；hover 效果仅作增强，并提供可见的 `focus-visible` 状态。
- 支持 Tab、Shift+Tab、Enter、Space；添加跳过导航入口、正确的标题层级和图片替代文本。
- 支持 200% 缩放、文本换行和内容选择；重点验证英文长标题、中文换行、长 URL。
- 语言切换持久化，更新 `html.lang`；校验存储值，存储不可用时仍可启动；无翻译时有明确回退。
- 遵循 `prefers-reduced-motion`；关闭 3D 后所有内容、路由和外链仍可使用。

## 4. 目标代码结构

按职责分层；下列为目标方向，在迁移中按实际需要创建，避免建立无用途的抽象。

```text
src/
  App.vue                     # 应用入口与路由出口
  router/                     # 路由、标题、滚动恢复、404
  layouts/                    # SiteLayout、DetailLayout
  components/
    layout/                   # PageContainer、SiteHeader、SiteFooter
    ui/                       # 共享链接/按钮、LanguageSwitch、TagList
    portfolio/                # ProjectCard、ProjectGrid、ProjectHeader
  views/                      # Home、About、Works、Projects、ProjectDetail、NotFound
  content/                    # 作品与项目数据，稳定 id、翻译 key、资源与链接
  composables/                # 语言、动画偏好、3D 生命周期等必要逻辑
  features/home-scene/         # 3D 场景、加载器、效果、资源销毁
  i18n/                       # 配置与中英文文案
  styles/                     # tokens、基础样式、排版规则
  utils/                      # 资源 URL 等少量纯函数
tests/                        # 关键逻辑、端到端和视觉回归
```

新内容模型和 3D 接口先通过 JSDoc 或局部 TypeScript 定义契约；全量 TypeScript 迁移独立评估，不阻塞适配修复。当前规模无需默认引入全局状态库。

## 5. 分阶段实施计划

### 阶段 0：基线与安全迁移准备（P0）

完成于 2026-09-18。24 张截图、Chrome/Edge 各 12 项正常冒烟通过与 6 项预期失败、20 次切页资源记录、三轮 Lighthouse 数据已归档。真机与其他浏览器仍按后续矩阵验证，外链受网络限制的条目明确标为未确认。

开发工具现要求 Node `>=22.19.0`（推荐 24 LTS），本地启动器已同步；这是 Lighthouse 13 的运行要求，业务依赖和页面源码保持原样。

1. 记录当前未提交改动，区分 Vite 升级、启动入口与后续重构，建立可回退的版本节点。
2. 清点正式页面、测试页、可用外链、缺失详情与资源；保留现有 URL 或建立重定向。
3. 运行现有站点，采集首页、作品列表、项目列表和详情的桌面/手机截图；记录溢出、导航、语言切换、控制台与网络错误。
4. 记录构建体积、Lighthouse 实验室数据、3D 帧率和多次路由切换后的资源占用，明确设备和测试条件。
5. 建立最小 Playwright 冒烟覆盖：首页访问、主要导航、语言切换与资源加载。原有缺陷记入清单，不把错误截图直接设为正确基线。

验收：形成可复现的问题清单、截图和性能记录；每个问题有页面、设备条件、期望行为及优先级。

### 阶段 1：运行稳定性与部署路径（P0 → P1）

完成于 2026-09-19，测试证据采集于 2026-09-18。Chrome/Edge 各 36 项正常通过、2 项语言持久化预期失败；根路径和项目子路径共 14 项静态托管检查通过，依赖审计为 0。20 次切页后首页保持 1 个活动 canvas/动画循环，离开后归零。代码尚未推送或部署；响应式 UI 重构进入阶段 2。

1. 移除 resize 整页刷新，修复匿名监听清理和计时器清理；普通布局优先交由 CSS。
2. 修复 Three.js 重复初始化、全局事件残留、卸载后异步回调；补齐 renderer、composer、pass、geometry、material、texture 的资源释放，防止重复销毁共享资源。
3. 为 3D 初始化失败、模型加载失败和 WebGL 不可用提供静态视觉回退；页面内容不等待模型加载。
4. 统一静态资源地址：导入资源交给 Vite；public 资源通过 BASE_URL 辅助函数寻址。验证根路径、子路径与详情页直达。
5. 修复主页/返回语义、详情路由、404 和滚动恢复；无站内历史时“返回”回到所属列表。
6. 审查 GitHub Pages 的 history 路由深链接方案：验证 404 回退的可行性；如使用 hash 路由，明确旧链接兼容策略，不能直接让既有 URL 失效。
7. 在兼容范围内修复 `vue-i18n` 审计问题；重新审计并验证翻译功能。Three.js 大版本升级另行处理，避免同时改变渲染结果。

验收：调整窗口不刷新；连续 20 次进入/离开首页不产生额外活动 canvas、动画循环或页面级事件；加载未完成即离开不会重新启动渲染；关键资源无 404。

### 阶段 2：共享布局与响应式 UI（P1）

完成于 2026-09-20。已移除固定页头占位、卡片测高、UUID 和全局 `--vsr`，补齐导航链接、键盘焦点及 44px 控件。11 种视口 × 7 个路由 × 2 种语言的布局检查通过；200% 文字放大与窄视口检查通过。此处不等同于所有浏览器的原生 200% 缩放或手机真机验收，待阶段 5 补齐。

1. 建立 token、reset、字体与容器规则，逐步替换全局 `--vsr` 耦合。
2. 实现共享页头、导航和语言切换，替换三个列表/介绍页的重复固定页头与占位 div。
3. 重构 ProjectCard，移除绝对定位正文、人工测高、重复 ID 和来自 Three.js 的 UUID 依赖。
4. 先迁移 Works 验证中英文长内容，再迁移 Projects 与 Kaiwu 展示，最后处理首页和详情页头。
5. 首页分离正文和 3D 装饰区域：手机保留清晰阅读顺序，取消用 `25vh` 空白为模型“让位”的做法。
6. 补齐键盘、触屏、焦点与缩放适配；统一装饰图片和内容图片语义。

验收：目标视口和两种语言下无正文遮挡、文字裁剪或页面级横向滚动；不能通过全局 `overflow-x: hidden` 掩盖布局错误；200% 缩放后关键功能可达。

### 阶段 3：内容、路由与组件边界（P1）

完成于 2026-09-20。14 条卡片与 Kaiwu 展示完成迁移，封面和既有外链无遗漏。Chrome／Edge 各 71 项正常通过、0 预期失败；11 项重复尺寸矩阵有意跳过。6 项单元测试和 14 项静态部署检查通过；TEST 仅开发模式可用，生产直达返回 404。阶段 2–3 已分别整理为本地提交，尚未推送或部署。

1. 将 Works/Projects 的重复模板提取为数据驱动渲染，定义 id、标题/介绍 key、封面、标签、日期、详情和外链字段。
2. 内链、外链与无详情状态明确建模；一次操作只发生一次导航，消除父子点击事件重复触发。
3. 统一 ProjectDetail 模板，完善 `/works/gcs` 的展示链路；未提供正文时显示清晰的待完善状态，不伪造内容。
4. 为 About 确定已有可展示信息；将首页 TEST 入口移至仅开发环境可见，正式路由行为有明确说明。
5. 完成所有导航和既有内容的中英文接入，持久化语言、更新页面标题。
6. 清理确认无引用的示例组件、死代码、调试日志和废弃样式；修正文档与命名，例如 `AniButtom`。

验收：新增作品主要通过增加内容数据完成；现有内容及外链对照清单无遗漏；刷新后语言保持；详情直达、前进后退与滚动恢复符合预期。

### 阶段 4：3D 增强与加载性能（P1 → P2）

1. 将场景改为实例接口，例如 `mount/resize/pause/resume/dispose`，由 Vue 组件持有，移除模块级可变场景状态。
2. 路由按需加载；首页 3D 动态导入，先显示正文和静态封面，再加载模型与效果。
3. 使用容器尺寸更新相机比例、renderer/composer 大小和效果分辨率；设置合理 DPR 上限。
4. 提供静态、低、中、高质量档：依据能力与运行表现选择，避免仅用设备名称或屏宽判断性能。
5. 页面隐藏时暂停渲染；处理恢复和 WebGL context 丢失；减少运动模式默认静态，并提供动画开关。
6. 评估高开销后处理链；不需要的效果不初始化，调试 GUI 仅在开发环境加载。
7. 图片提供合适尺寸、现代格式与回退，非首屏图片懒加载，预留尺寸避免跳动；评估模型压缩，验证解码成本与视觉质量后再采用。

验收：非首页不加载首页 3D 代码与模型；关闭动画时不启动渲染循环；中低档真机运行不稳定时可降级且不影响操作。性能预算见下表。

### 阶段 5：回归、工具与交付（P1）

1. 补齐关键路径端到端与视觉回归，视觉截图固定语言、字体和动画状态；不为简单样式机械增加单元测试。
2. 为有分支逻辑的内容映射、URL 生成、语言存储与 3D 卸载状态增加针对性测试。
3. 建立 lint、构建和浏览器检查流程；CI 至少验证 Windows/Linux 安装构建及浏览器矩阵，macOS/iOS 进行对应实机验证。
4. 保留 Windows 双击启动，补充 macOS/Linux 的标准命令行启动说明；兼容 Node 要求写入 README 和 CI。
5. 明确构建产物不作为源代码维护，确认部署方式后移除对旧 dist 的跟踪，通过 CI 生成发布产物。
6. 先生成预览供核对，再执行已获授权的上线步骤；记录部署路径、回滚方式与已知限制。

验收：全新检出后按 README 能安装、启动和构建；自动检查通过；真机结果有记录；重大问题关闭后才发布。

## 6. 验证矩阵

各尺寸至少覆盖首页、Works、Projects 和一个详情页；桌面与手机核心路径覆盖中英文。浏览器具体版本在阶段 0 固定记录，以项目实际访客需求调整支持范围。

| 维度 | 覆盖范围 | 重点 |
| --- | --- | --- |
| 窄屏与手机 | 320×568、360×800、390×844、430×932 | 无横向溢出，长标题可读，按钮可点，3D 不遮正文 |
| 平板与旋转 | 768×1024、1024×768，手机横屏 | 横竖屏切换不刷新，布局不中断，页头不挤压内容 |
| 桌面与超宽 | 1280×720、1440×900、1920×1080、2560×1440 | 正文宽度受控，窗口拖动与分屏布局稳定 |
| 桌面浏览器 | Windows Chrome/Edge/Firefox；macOS Safari/Chrome | 导航、字体、CSS 和 WebGL 表现 |
| 移动真机 | iPhone Safari、Android Chrome，至少一台中低档设备 | 地址栏、安全区、触控、旋转、内存与 GPU |
| 自动化引擎 | Chromium、Firefox、WebKit | 关键流程回归；WebKit 模拟不代替 iOS 真机 |
| 可访问性 | 键盘、200% 缩放、减少运动、屏幕阅读器抽查 | 焦点可见、顺序正确、无仅 hover 可用的功能 |
| 异常状态 | 慢网、图片/模型失败、禁用 WebGL、存储受限 | 内容可访问、有回退、无无限加载 |
| 部署 | 根路径、GitHub Pages 子路径、详情直达/刷新 | 无资源 404，导航和回退路径正确 |

## 7. 性能预算与完成标准

以下为拟定目标，不是当前实测结论。阶段 0 固定设备、网络和冷缓存条件；阶段 4 在相同条件下比较。若无法达标，记录原因与取舍，不能只调高警告阈值。

| 项目 | 拟定目标 / 测量方法 |
| --- | --- |
| 内容首屏 | 在不等待 3D 的情况下可阅读和操作；移动端约定条件下 LCP ≤ 2.5s、CLS ≤ 0.1，至少取 3 次实验室测试中位数 |
| 交互 | 以真实访问数据的 INP ≤ 200ms 为后续目标；无真实数据时记录交互实验室测量，不宣称已通过真实用户指标 |
| 代码 | 核心页面初始 JS gzip 目标 ≤ 150 kB，不含延迟加载的 3D；单独列出 3D 与模型预算，避免把下载成本藏在分包后 |
| 图片 | 常规列表缩略图目标 ≤ 200 kB/张，首屏图片按显示尺寸加载；特殊高精度作品单独说明 |
| 动画 | 约定中档移动设备低质量档目标稳定 30fps；未达标时自动/手动切换静态；后台不持续渲染 |
| 资源生命周期 | 重复切页后 renderer 数量、事件与动画循环不累积；预热后内存无持续上升趋势，不能仅凭一次快照判定 |
| 功能与视觉 | 矩阵内核心流程通过，无未处理运行错误；无关键内容丢失、遮挡或资源 404 |
| 工程 | 安装、lint、必要测试、build 通过；依赖审计高危项修复或有明确处置记录 |

## 8. 顺序、工作量与里程碑

单人实施的初步估算为 13–20 个工作日，不含新增文案、用户反馈等待和设备采购。阶段 0 后根据实际页面与设备结果修订。

| 阶段 | 估算 | 依赖 / 交付节点 |
| --- | --- | --- |
| 0 基线 | 1–2 日 | 输出问题清单与截图、性能基线 |
| 1 稳定性 | 2–3 日 | 基线完成后处理；产出可稳定导航和缩放的版本 |
| 2 响应式 UI | 3–4 日 | 稳定性修复后；手机、平板、桌面共用布局可用 |
| 3 内容与路由 | 2–3 日 | 使用阶段 2 的共享组件；完成页面迁移 |
| 4 3D 与性能 | 3–5 日 | 基于稳定的首页容器及阶段 1 生命周期修复 |
| 5 回归交付 | 2–3 日 | 各阶段持续补测试，最后集中真机回归与发布检查 |

每个阶段提交范围清晰的变更，附受影响页面、验证结果和剩余问题。遇到设计变化先更新对应验收图与目标，不将功能重构和未经确认的全站视觉改版混为一体。

## 9. 待核对事项与默认决策

- 视觉方向：默认保留当前品牌和深色风格，重做布局与交互规则；不新增完整浅色主题。
- 首页 3D：默认保留为渐进增强，移动端允许低质量或静态展示。
- 缺失内容：About、GCS 与其他未完成详情需后续提供内容；这不阻塞基础布局、路由和性能修复。
- 部署权限和方式：默认目标仍为 GitHub Pages；先核对仓库 Pages 设置，再决定发布工作流和深链接方案。
- 浏览器范围：以现代浏览器为目标；若需要兼容较旧 iOS/Android，阶段 0 固定最低版本并复核 Vite 的构建目标与功能回退。
- 真机资源：无法取得的设备标记为“未验证”，不以桌面模拟测试代替通过结论。

建议首先实施阶段 0–1，随后以 Works 页面作为响应式组件的首个迁移样本，验证通过后推广到其余页面。
